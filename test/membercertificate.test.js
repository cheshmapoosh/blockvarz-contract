const MemberCertificate = artifacts.require("MemberCertificate");

contract("MemberCertificate", accounts => {
  // it("Initial certificate count should be 0", () =>
  //   MemberCertificate.deployed()
  //     .then(instance => instance.countCertificates.call())
  //     .then(count => {
  //       assert.equal(
  //         count.toNumber(),
  //         0,
  //         "initial certificate count isn't 0"
  //       );
  //     }));

  // it("Deploy certificate & check count shuld be 1", () => {
  //   let contract;
  //   let address = accounts[0];
  //   let imageHash = "159753654852";
  //   let title = "hasan agha";

  //   return MemberCertificate.deployed()
  //     .then(instance => {
  //       contract = instance;
  //       instance.setCertificate.call(address, imageHash, title);
  //     })
  //     .then(() => contract.countCertificates.call())
  //     .then(certificateCount => {
  //       console.log('certificate count: ', certificateCount.toNumber());
  //       // assert.equal(
  //       //   certificateCount,
  //       //   1,
  //       //   "certificate count isn't 1"
  //       // );
  //     })
  // });
   

});
